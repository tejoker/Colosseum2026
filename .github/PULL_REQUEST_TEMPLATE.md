## Summary

<!-- What and why, one or two sentences. -->

## Checklist

- [ ] Tests pass (`make test` for Rust; `make python-test` / `make sdk-test` / `make dashboard-test` as applicable)
- [ ] `cargo fmt --check` and `cargo clippy -- -D warnings` clean (release gate enforces both)
- [ ] Docs updated if behavior changed
