[package]
name = "sauron-transparent-types"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"

[dependencies]
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }

